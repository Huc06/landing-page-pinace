---
title: Aftermath
description: DEX aggregator + stable pools. Next-up venue — best-route swaps inside policy bounds.
---

<Callout type="warn">**Status: planned.** DEX aggregator integration on the next-wave list.</Callout>

Aftermath ships an aggregator that routes across DeepBook + Cetus +
other venues. From Pinace's POV the action shape is the same as a
single swap — the aggregator just picks the route. Policies apply
once (on `amount_in`), not per hop.

- [Aftermath docs](https://docs.aftermath.finance)
- [Aftermath TS SDK](https://www.npmjs.com/package/aftermath-ts-sdk)
