# Lift transitive peer deps (vite, rolldown, etc.) so fumadocs-mdx's
# postinstall script can resolve them — strict pnpm hoisting hides
# them from the package's own bin. Standard for fumadocs apps.
shamefully-hoist=true
auto-install-peers=true
